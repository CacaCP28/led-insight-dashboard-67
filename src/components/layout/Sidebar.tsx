// This is a placeholder file that redirects to CustomSidebar
// We're keeping this file for backward compatibility with existing imports

import CustomSidebar from "./CustomSidebar";

export default CustomSidebar;
